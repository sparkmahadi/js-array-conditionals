var iphonePrice = 79000;
var myBudget = 9500;

// if iphone price is less than my budge. I will buy the iphone.

/**
 * if (condition) {
 * //will execute if the condition is true}\
 * 
 * alternative option
 * else (condition) {
 * execution}
 */

if (iphonePrice < myBudget) {
    console.log('iphone kine futani marbo');
}
else {
    console.log('bashai choila ashmu');
}